/**
 * Repräsentiert einen Studenten.
 */
class Student {
  /**
   * @param {string} name Name des Studenten
   * @param {number} age Alter des Studenten
   */
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }
}